id: https://vocab.account.gov.uk/linkml/ipv-fraud-cri-end
name: ipv-fraud-cri-end
description: Description for audit event IPV_FRAUD_CRI_END
prefixes:
  linkml: https://w3id.org/linkml/
  di_vocab: https://vocab.account.gov.uk/v1/
imports:
- ./slots
default_curi_maps:
- semweb_context
default_prefix: di_vocab
default_range: string
classes:
  IpvFraudCriEndAuditEventClass:
    description: >
      A non-common request has been received
    slots:
    - event_name
    - component_id
    - user
    slot_usage:
      user:
        range: IpvFraudCriEndUserClass
      event_name:
        pattern: ^IPV_FRAUD_CRI_END$
  IpvFraudCriEndUserClass:
    slots:
    - govuk_signin_journey_id
    - ip_address
    - persistent_session_id
    - session_id
    - user_id
