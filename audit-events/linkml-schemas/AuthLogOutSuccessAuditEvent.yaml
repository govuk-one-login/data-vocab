id: https://vocab.account.gov.uk/linkml/auth-log-out-success
name: auth-log-out-success
description: Description for audit event AUTH_LOG_OUT_SUCCESS
prefixes:
  linkml: https://w3id.org/linkml/
  di_vocab: https://vocab.account.gov.uk/v1/
imports:
- ./slots
default_curi_maps:
- semweb_context
default_prefix: di_vocab
default_range: string
classes:
  AuthLogOutSuccessAuditEventClass:
    slots:
    - event_name
    - client_id
    - component_id
    - user
    slot_usage:
      user:
        range: AuthLogOutSuccessUserClass
      event_name:
        pattern: ^AUTH_LOG_OUT_SUCCESS$
  AuthLogOutSuccessUserClass:
    slots:
    - ip_address
    - persistent_session_id
    - session_id
