
id: https://vocab.account.gov.uk/linkml/auth-phone-code-sent-for-test-client
name: auth-phone-code-sent-for-test-client
description: Description for audit event AUTH_PHONE_CODE_SENT_FOR_TEST_CLIENT
prefixes:
  linkml: https://w3id.org/linkml/
  di_vocab: https://vocab.account.gov.uk/v1/
imports:
  - ./slots
default_curi_maps:
  - semweb_context 
default_prefix: di_vocab
default_range: string
classes:
  AuthPhoneCodeSentForTestClientAuditEventClass:
    slots:
      - event_name
      - client_id
      - component_id
      - user
    slot_usage:
      user:
        range: AuthPhoneCodeSentForTestClientUserClass

  AuthPhoneCodeSentForTestClientUserClass:
    slots:
      - email
      - ip_address
      - phone
      - session_id
