id: https://vocab.account.gov.uk/linkml/auth-phone-invalid-code-request
name: auth-phone-invalid-code-request
description: Description for audit event AUTH_PHONE_INVALID_CODE_REQUEST
prefixes:
  linkml: https://w3id.org/linkml/
  di_vocab: https://vocab.account.gov.uk/v1/
imports:
- ./slots
default_curi_maps:
- semweb_context
default_prefix: di_vocab
default_range: string
classes:
  AuthPhoneInvalidCodeRequestAuditEventClass:
    slots:
    - event_name
    - client_id
    - component_id
    - user
    slot_usage:
      user:
        range: AuthPhoneInvalidCodeRequestUserClass
      event_name:
        pattern: ^AUTH_PHONE_INVALID_CODE_REQUEST$
  AuthPhoneInvalidCodeRequestUserClass:
    slots:
    - email
    - ip_address
    - phone
    - session_id
