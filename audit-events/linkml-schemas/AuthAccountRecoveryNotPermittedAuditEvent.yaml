id: https://vocab.account.gov.uk/linkml/auth-account-recovery-not-permitted
name: auth-account-recovery-not-permitted
description: >-
  Description for audit event AUTH_ACCOUNT_RECOVERY_NOT_PERMITTED
prefixes:
  linkml: https://w3id.org/linkml/
  di_vocab: https://vocab.account.gov.uk/v1/

imports:
  - ./slots

default_curi_maps:
  - semweb_context  
default_prefix: di_vocab
default_range: string

classes:
  AuthAccountRecoveryNotPermittedAuditEventClass:
    slots:
      - event_name
      - client_id
      - component_id
      - user

  AuthAccountRecoveryNotPermittedUserClass:
    slots:
      - email
      - ip_address
      - persistent_session_id
      - session_id
      - transaction_id
      - user_id

slots:
  user:
    range: AuthAccountRecoveryNotPermittedUserClass
    description: Description for user
