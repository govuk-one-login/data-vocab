id: https://vocab.account.gov.uk/linkml/auth-phone-code-sent
name: auth-phone-code-sent
description: >-
  Description for audit event AUTH_PHONE_CODE_SENT
prefixes:
  linkml: https://w3id.org/linkml/
  di_vocab: https://vocab.account.gov.uk/v1/

imports:
  - ./slots

default_curi_maps:
  - semweb_context  
default_prefix: di_vocab
default_range: string

classes:
  AuthPhoneCodeSentAuditEventClass:
    slots:
      - event_name
      - client_id
      - component_id
      - user

  AuthPhoneCodeSentUserClass:
    slots:
      - email
      - ip_address
      - phone
      - session_id

slots:
  user:
    range: AuthPhoneCodeSentUserClass
    description: Description for user
