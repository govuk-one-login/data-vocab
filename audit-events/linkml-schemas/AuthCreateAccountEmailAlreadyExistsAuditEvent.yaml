
id: https://vocab.account.gov.uk/linkml/auth-create-account-email-already-exists
name: auth-create-account-email-already-exists
description: Description for audit event AUTH_CREATE_ACCOUNT_EMAIL_ALREADY_EXISTS
prefixes:
  linkml: https://w3id.org/linkml/
  di_vocab: https://vocab.account.gov.uk/v1/
imports:
  - ./slots
default_curi_maps:
  - semweb_context 
default_prefix: di_vocab
default_range: string
classes:
  AuthCreateAccountEmailAlreadyExistsAuditEventClass:
    slots:
      - event_name
      - client_id
      - component_id
      - user
    slot_usage:
      user:
        range: AuthCreateAccountEmailAlreadyExistsUserClass

  AuthCreateAccountEmailAlreadyExistsUserClass:
    slots:
      - email
      - govuk_signin_journey_id
      - ip_address
      - persistent_session_id
      - session_id
