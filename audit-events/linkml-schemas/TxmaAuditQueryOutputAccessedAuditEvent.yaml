id: https://vocab.account.gov.uk/linkml/txma-audit-query-output-accessed
name: txma-audit-query-output-accessed
description: Description for audit event TXMA_AUDIT_QUERY_OUTPUT_ACCESSED
prefixes:
  linkml: https://w3id.org/linkml/
  di_vocab: https://vocab.account.gov.uk/v1/
imports:
- ./slots
default_curi_maps:
- semweb_context
default_prefix: di_vocab
default_range: string
classes:
  TxmaAuditQueryOutputAccessedAuditEventClass:
    description: >
      Requester access data output generated
    slots:
    - event_name
    - component_id
    - extensions
    slot_usage:
      extensions:
        range: TxmaAuditQueryOutputAccessedExtensionsClass
      event_name:
        pattern: ^TXMA_AUDIT_QUERY_OUTPUT_ACCESSED$
  TxmaAuditQueryOutputAccessedExtensionsClass:
    slots:
    - ticket_details
